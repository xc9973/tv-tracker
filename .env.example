# TMDB API Key (必填)
# 从 https://www.themoviedb.org/settings/api 获取
TMDB_API_KEY=your_tmdb_api_key_here

# Telegram Bot 配置 (必填)
# 1. 找 @BotFather 创建 Bot 获取 Token
# 2. 找 @userinfobot 获取你的个人 Chat ID (管理员)
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_personal_chat_id_here

# 频道 ID (可选，用于发送每日日报)
# 如果不配置，日报会发送到管理员私聊
TELEGRAM_CHANNEL_ID=-1001234567890

# 日报发送时间 (可选，默认 08:00)
REPORT_TIME=09:00

# 关闭超时时间（秒）(可选，默认 5)
SHUTDOWN_TIMEOUT=5

# 静态文件目录 (可选，默认 ./web/simple)
STATIC_DIR=./web/simple

# Web 界面配置
WEB_ENABLED=true
WEB_LISTEN_ADDR=:18080

# Web界面访问密码 (重要!)
# 这是您访问Web界面时需要输入的密码
# 建议使用强密码,例如: MySecurePass2024!
WEB_API_TOKEN=MySecurePass2024
