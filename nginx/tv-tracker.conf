# TV Tracker Nginx 配置
# 复制到: /etc/nginx/sites-available/tv-tracker.conf
# 启用: ln -s /etc/nginx/sites-available/tv-tracker.conf /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name tv.yourdomain.com;  # 改成你的域名

    # 重定向到 HTTPS (如果配置了 SSL)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持 (如果需要)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 静态资源缓存
    location /assets/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_cache_valid 200 7d;
        add_header Cache-Control "public, max-age=604800";
    }
}

# HTTPS 配置 (使用 certbot 自动生成证书后启用)
# server {
#     listen 443 ssl http2;
#     server_name tv.yourdomain.com;
#
#     ssl_certificate /etc/letsencrypt/live/tv.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tv.yourdomain.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
